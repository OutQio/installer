# Browser-side installer

A self-contained HTML page that uses the File System Access API to write a private GitHub repo's contents to a folder of your choice. Useful when you can only run a browser (no shell access) on the target machine.

Open: https://outqio.github.io/installer/
